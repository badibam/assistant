{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": "{{SHORT_LENGTH}}",
      "description": "Zone name - displayed in UI"
    },
    "description": {
      "type": "string",
      "maxLength": "{{MEDIUM_LENGTH}}",
      "description": "Optional zone description"
    }
  },
  "required": ["name"],
  "additionalProperties": false
}